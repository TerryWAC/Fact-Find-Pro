/**
 * Public FactFind routes. The branded shell (header, colours, footer) lives one
 * level down in [type]/[slug]/layout.tsx, where the adviser is known.
 */
export default function PublicFactFindLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
